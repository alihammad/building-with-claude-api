[project]
name = "building-with-claude-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ali", email = "ali.hammadbaig@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
building-with-claude-api = "building_with_claude_api:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
